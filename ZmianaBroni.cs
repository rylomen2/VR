using UnityEngine;
using System.Collections;

public class ZmianaBroni : MonoBehaviour {
	private TypAtaku bron;
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		if(Input.GetAxis("Mouse ScrollWheel") != 0f ){

		}
	
	}
}
